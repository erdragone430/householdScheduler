# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next.js
/.next/
/out/

# production
/build

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example
!.env.alpha.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# uploaded assets (user content, CI-placed meta files)
public/uploads
public/meta/

# favicon (supplied by external assets, not committed)
public/favicon.*

# app-brand logo images (not committed, placed by CI/deploy)
public/logos/